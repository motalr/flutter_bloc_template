import 'package:flutter/material.dart';

class ThemeConfig {
  static const String fontFamily = 'Avenir';

  static const Color lightThemeBackground = Colors.white;
  static const Color darkThemeBackground = Color.fromARGB(255, 28, 40, 51);

  static const Color primaryColor = Colors.blueAccent;
  static const Color secondaryColor = Colors.blueGrey;
  static const Color warningColor = Colors.orangeAccent;
  static const Color errorColor = Colors.redAccent;
  static const Color mutedColor = Color.fromARGB(255, 112, 109, 109);
  static const Color googleRedColor = Color.fromARGB(255, 219, 58, 65);

  static const Color textPrimaryLightModeColor = Colors.black;
  static const Color textPrimaryDarkModeColor = Colors.white;
}
